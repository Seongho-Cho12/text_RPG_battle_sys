============================================================
[PASS] tests/test_phase5_movement.py::test_phase5_engage_moves_actor_into_target_group_and_updates_membership
------------------------------------------------------------
[EXPERIMENT]
TITLE: ENGAGE가 대상(target)의 그룹으로 합류하며, group_id와 groups 멤버십이 일관되게 갱신되는지 검증
    SETUP:
      - Allies:
        - A1 (lv1, agi=6, wis=5)
        - A2 (lv1, agi=5, wis=5)
      - Enemies:
        - E1 (lv1, agi=4, wis=5)
      - 초기 상태:
        - A1/A2는 같은 그룹(아군 그룹)
        - E1은 적군 그룹
      - 이동 규칙(ENGAGE):
        - actor는 target이 속한 그룹으로 합류한다.
    STEPS:
      1) create_battle([A1,A2],[E1])
      2) ENGAGE: engage(bs, actor=A1, target=E1) 호출
      3) A1이 E1과 same_group이 되었는지 확인
      4) A2는 여전히 A1과 같은 그룹이 아닌지(즉 A2는 아군 그룹에 남았는지) 확인
      5) groups 멤버십이 중복/누락 없이 정확한지 확인
    EXPECTED:
      - A1.group_id == E1.group_id
      - same_group(A1,E1) == True
      - same_group(A1,A2) == False  (A1이 적군 그룹으로 이동했으므로)
      - 전체 멤버십에 A1/A2/E1이 각각 정확히 1회씩 존재

============================================================
[PASS] tests/test_phase5_movement.py::test_phase5_disengage_creates_new_solo_group_and_removes_empty_group
------------------------------------------------------------
[EXPERIMENT]
TITLE: DISENGAGE가 actor 단독 그룹을 생성하고, 기존 그룹이 비면 제거되는지 검증
    SETUP:
      - Allies:
        - A1 (lv1, agi=6, wis=5)
        - A2 (lv1, agi=5, wis=5)
      - Enemies:
        - E1 (lv1, agi=4, wis=5)
      - 초기 상태:
        - A1/A2는 같은 그룹(아군 그룹)
    STEPS:
      1) create_battle([A1,A2],[E1])
      2) DISENGAGE: new_gid = disengage(bs, actor=A1)
      3) A1이 A2와 다른 그룹이 되었는지 확인
      4) 새 그룹(new_gid)의 멤버가 A1 단독인지 확인
      5) 전체 멤버십이 중복/누락 없이 정확한지 확인
    EXPECTED:
      - same_group(A1,A2) == False
      - bs.groups[new_gid] == [A1]
      - A2는 기존 아군 그룹에 남아있음
      - 모든 멤버(A1,A2,E1)가 정확히 1회씩만 존재

============================================================
[PASS] tests/test_phase5_movement.py::test_phase5_engage_raises_if_already_same_group
------------------------------------------------------------
[EXPERIMENT]
TITLE: ENGAGE가 이미 같은 그룹인 경우 예외를 발생시키는지 검증(의미 없는 이동 방지)
    SETUP:
      - Allies: A1, A2 (둘 다 같은 아군 그룹)
      - 규칙:
        - ENGAGE는 "대상 그룹에 합류"인데, 이미 같은 그룹이면 이동이 발생하지 않는다.
        - 이 케이스는 버그 조기 발견을 위해 예외로 처리한다.
    STEPS:
      1) create_battle([A1,A2],[])
      2) engage(bs, actor=A1, target=A2) 호출
    EXPECTED:
      - ValueError 발생

