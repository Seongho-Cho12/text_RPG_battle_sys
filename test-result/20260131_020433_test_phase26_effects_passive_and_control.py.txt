============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_attacker_hit_effects_apply_and_expire[Confusion--20]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 공격자(attacker)에게 걸린 상태이상이 HIT 지수에 반영되고,
              tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.

    검증 대상(표 기준):
    - Confusion: HIT -20
    - Fear: HIT -10
    - Blind: HIT -40
    - Slow: HIT -10

    시나리오:
    - baseline indices 측정
    - attacker에 effect를 ticks=1로 부여
    - hit가 baseline + delta_hit 인지 확인
    - end_turn 1회 => effect 삭제
    - hit가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_attacker_hit_effects_apply_and_expire[Fear--10]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 공격자(attacker)에게 걸린 상태이상이 HIT 지수에 반영되고,
              tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.

    검증 대상(표 기준):
    - Confusion: HIT -20
    - Fear: HIT -10
    - Blind: HIT -40
    - Slow: HIT -10

    시나리오:
    - baseline indices 측정
    - attacker에 effect를 ticks=1로 부여
    - hit가 baseline + delta_hit 인지 확인
    - end_turn 1회 => effect 삭제
    - hit가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_attacker_hit_effects_apply_and_expire[Blind--40]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 공격자(attacker)에게 걸린 상태이상이 HIT 지수에 반영되고,
              tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.

    검증 대상(표 기준):
    - Confusion: HIT -20
    - Fear: HIT -10
    - Blind: HIT -40
    - Slow: HIT -10

    시나리오:
    - baseline indices 측정
    - attacker에 effect를 ticks=1로 부여
    - hit가 baseline + delta_hit 인지 확인
    - end_turn 1회 => effect 삭제
    - hit가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_attacker_hit_effects_apply_and_expire[Slow--10]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 공격자(attacker)에게 걸린 상태이상이 HIT 지수에 반영되고,
              tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.

    검증 대상(표 기준):
    - Confusion: HIT -20
    - Fear: HIT -10
    - Blind: HIT -40
    - Slow: HIT -10

    시나리오:
    - baseline indices 측정
    - attacker에 effect를 ticks=1로 부여
    - hit가 baseline + delta_hit 인지 확인
    - end_turn 1회 => effect 삭제
    - hit가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_evade_effects_apply_and_expire[Confusion--5]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에게 걸린 상태이상이 EVADE 지수에 반영되고,
              tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.

    검증 대상(표 기준):
    - Confusion: EVADE -5
    - Fear: EVADE -50
    - Slow: EVADE -10
    - Bind: EVADE -50

    시나리오:
    - baseline indices 측정
    - defender에 effect ticks=1 부여
    - evade가 baseline + delta_evade 인지 확인
    - end_turn 1회 => effect 삭제
    - evade가 baseline으로 복귀하는지 확인


============================================================
[FAIL] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_evade_effects_apply_and_expire[Fear--50]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에게 걸린 상태이상이 EVADE 지수에 반영되고,
              tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.

    검증 대상(표 기준):
    - Confusion: EVADE -5
    - Fear: EVADE -50
    - Slow: EVADE -10
    - Bind: EVADE -50

    시나리오:
    - baseline indices 측정
    - defender에 effect ticks=1 부여
    - evade가 baseline + delta_evade 인지 확인
    - end_turn 1회 => effect 삭제
    - evade가 baseline으로 복귀하는지 확인


[TRACEBACK]
effect_id = 'Fear', delta_evade = -50

    @pytest.mark.parametrize(
        "effect_id, delta_evade",
        [
            ("Confusion", -5),
            ("Fear", -50),
            ("Slow", -10),
            ("Bind", -50),
        ],
    )
    def test_phase26_defender_evade_effects_apply_and_expire(effect_id: str, delta_evade: int):
        """
        [Phase 26] 방어자(defender)에게 걸린 상태이상이 EVADE 지수에 반영되고,
                  tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.
    
        검증 대상(표 기준):
        - Confusion: EVADE -5
        - Fear: EVADE -50
        - Slow: EVADE -10
        - Bind: EVADE -50
    
        시나리오:
        - baseline indices 측정
        - defender에 effect ticks=1 부여
        - evade가 baseline + delta_evade 인지 확인
        - end_turn 1회 => effect 삭제
        - evade가 baseline으로 복귀하는지 확인
        """
        eng, bs, A, E = _mk_engine_1v1()
    
        base = _snap(bs, A, E, crit_stat="STR")
        _set_effect(bs, E, effect_id, ticks=1)
    
        after = _snap(bs, A, E, crit_stat="STR")
>       assert after.evade == base.evade + delta_evade
E       assert 0 == (8 + -50)
E        +  where 0 = IndicesSnap(hit=50, evade=0, weak=34, strong=15, critical=3).evade
E        +  and   8 = IndicesSnap(hit=50, evade=8, weak=34, strong=15, critical=3).evade

tests\test_phase26_effects_passive_and_control.py:165: AssertionError
============================================================
[FAIL] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_evade_effects_apply_and_expire[Slow--10]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에게 걸린 상태이상이 EVADE 지수에 반영되고,
              tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.

    검증 대상(표 기준):
    - Confusion: EVADE -5
    - Fear: EVADE -50
    - Slow: EVADE -10
    - Bind: EVADE -50

    시나리오:
    - baseline indices 측정
    - defender에 effect ticks=1 부여
    - evade가 baseline + delta_evade 인지 확인
    - end_turn 1회 => effect 삭제
    - evade가 baseline으로 복귀하는지 확인


[TRACEBACK]
effect_id = 'Slow', delta_evade = -10

    @pytest.mark.parametrize(
        "effect_id, delta_evade",
        [
            ("Confusion", -5),
            ("Fear", -50),
            ("Slow", -10),
            ("Bind", -50),
        ],
    )
    def test_phase26_defender_evade_effects_apply_and_expire(effect_id: str, delta_evade: int):
        """
        [Phase 26] 방어자(defender)에게 걸린 상태이상이 EVADE 지수에 반영되고,
                  tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.
    
        검증 대상(표 기준):
        - Confusion: EVADE -5
        - Fear: EVADE -50
        - Slow: EVADE -10
        - Bind: EVADE -50
    
        시나리오:
        - baseline indices 측정
        - defender에 effect ticks=1 부여
        - evade가 baseline + delta_evade 인지 확인
        - end_turn 1회 => effect 삭제
        - evade가 baseline으로 복귀하는지 확인
        """
        eng, bs, A, E = _mk_engine_1v1()
    
        base = _snap(bs, A, E, crit_stat="STR")
        _set_effect(bs, E, effect_id, ticks=1)
    
        after = _snap(bs, A, E, crit_stat="STR")
>       assert after.evade == base.evade + delta_evade
E       assert 0 == (8 + -10)
E        +  where 0 = IndicesSnap(hit=50, evade=0, weak=34, strong=15, critical=3).evade
E        +  and   8 = IndicesSnap(hit=50, evade=8, weak=34, strong=15, critical=3).evade

tests\test_phase26_effects_passive_and_control.py:165: AssertionError
============================================================
[FAIL] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_evade_effects_apply_and_expire[Bind--50]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에게 걸린 상태이상이 EVADE 지수에 반영되고,
              tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.

    검증 대상(표 기준):
    - Confusion: EVADE -5
    - Fear: EVADE -50
    - Slow: EVADE -10
    - Bind: EVADE -50

    시나리오:
    - baseline indices 측정
    - defender에 effect ticks=1 부여
    - evade가 baseline + delta_evade 인지 확인
    - end_turn 1회 => effect 삭제
    - evade가 baseline으로 복귀하는지 확인


[TRACEBACK]
effect_id = 'Bind', delta_evade = -50

    @pytest.mark.parametrize(
        "effect_id, delta_evade",
        [
            ("Confusion", -5),
            ("Fear", -50),
            ("Slow", -10),
            ("Bind", -50),
        ],
    )
    def test_phase26_defender_evade_effects_apply_and_expire(effect_id: str, delta_evade: int):
        """
        [Phase 26] 방어자(defender)에게 걸린 상태이상이 EVADE 지수에 반영되고,
                  tick 감소로 상태이상이 삭제되면 더 이상 반영되지 않아야 한다.
    
        검증 대상(표 기준):
        - Confusion: EVADE -5
        - Fear: EVADE -50
        - Slow: EVADE -10
        - Bind: EVADE -50
    
        시나리오:
        - baseline indices 측정
        - defender에 effect ticks=1 부여
        - evade가 baseline + delta_evade 인지 확인
        - end_turn 1회 => effect 삭제
        - evade가 baseline으로 복귀하는지 확인
        """
        eng, bs, A, E = _mk_engine_1v1()
    
        base = _snap(bs, A, E, crit_stat="STR")
        _set_effect(bs, E, effect_id, ticks=1)
    
        after = _snap(bs, A, E, crit_stat="STR")
>       assert after.evade == base.evade + delta_evade
E       assert 0 == (8 + -50)
E        +  where 0 = IndicesSnap(hit=50, evade=0, weak=34, strong=15, critical=3).evade
E        +  and   8 = IndicesSnap(hit=50, evade=8, weak=34, strong=15, critical=3).evade

tests\test_phase26_effects_passive_and_control.py:165: AssertionError
============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Burned--10-STR]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Frostbite--10-STR]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Frozen--15-STR]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Burned--10-INT]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Frostbite--10-INT]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Frozen--15-INT]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Stun--5-STR]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Stun-0-INT]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Paralysis-0-STR]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Paralysis--5-INT]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Corruption-0-STR]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_defender_weak_weight_effects_apply_and_expire[Corruption--15-INT]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 방어자(defender)에 걸린 상태이상이 "약공 피격 지수"에 반영되는지 검증한다.
              구현상 defender의 crit.weak 가중치에 delta가 합산되는 것으로 테스트한다.

    검증 대상(표 기준):
    - Burned: 약공 피격 -10 (항상)
    - Frostbite: 약공 피격 -10 (항상)
    - Frozen: 약공 피격 -15 (항상)
    - Stun: 물리 약공 피격 -5 (crit_stat STR/AGI일 때만)
    - Paralysis: 마법 약공 피격 -5 (crit_stat INT/WIS일 때만)
    - Corruption: 마법 약공 피격 -15 (crit_stat INT/WIS일 때만)

    시나리오:
    - baseline crit.weak 측정
    - defender에 effect ticks=1 부여
    - crit_stat 조건에 따라 weak가 baseline + delta_weak 인지 확인
    - end_turn 1회 => effect 삭제
    - weak가 baseline으로 복귀하는지 확인


============================================================
[PASS] tests/test_phase26_effects_passive_and_control.py::test_phase26_weakness_increases_attacker_weak_on_physical_only_and_expires
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] Weakness:
      - 자신의 물리 공격에 대한 약공 지수 +20
      - 즉, attacker가 Weakness일 때 crit_stat이 STR/AGI인 공격에서만
        attacker의 crit.weak 가중치가 +20 되어야 한다.

    시나리오:
    - baseline(STR)과 baseline(INT)의 attacker weak를 각각 측정
    - attacker에 Weakness ticks=1 부여
    - STR에서는 weak +20, INT에서는 변화 없음
    - end_turn 1회로 삭제되면 두 경우 모두 baseline으로 복귀


============================================================
[FAIL] tests/test_phase26_effects_passive_and_control.py::test_phase26_action_blocking_effects_block_any_skill[Stun]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 행동 불가 상태이상은 어떤 스킬이든 사용을 막아야 한다.
    대상:
    - Stun / Paralysis / Frozen

    시나리오:
    - actor(A)에 effect ticks=2 부여
    - '간단 데미지 스킬' 시전 시도
    기대:
    - SKILL_BLOCKED_BY_EFFECT 이벤트가 발생
    - target HP 변화 없음 (실제 step 적용이 일어나지 않음)


[TRACEBACK]
effect_id = 'Stun'

    @pytest.mark.parametrize("effect_id", ["Stun", "Paralysis", "Frozen"])
    def test_phase26_action_blocking_effects_block_any_skill(effect_id: str):
        """
        [Phase 26] 행동 불가 상태이상은 어떤 스킬이든 사용을 막아야 한다.
        대상:
        - Stun / Paralysis / Frozen
    
        시나리오:
        - actor(A)에 effect ticks=2 부여
        - '간단 데미지 스킬' 시전 시도
        기대:
        - SKILL_BLOCKED_BY_EFFECT 이벤트가 발생
        - target HP 변화 없음 (실제 step 적용이 일어나지 않음)
        """
        eng, bs, A, E = _mk_engine_1v1()
    
        _set_effect(bs, A, effect_id, ticks=2)
    
        before = bs.combatants[E].hp
        skill = _simple_damage_skill(A, E, action_type="MAIN", crit_stat="STR")
    
>       out = eng.apply_skill(bs, skill)

tests\test_phase26_effects_passive_and_control.py:395: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <battle_system.engine.engine.BattleEngine object at 0x000001DAA25A0BC0>
bs = BattleState(defs={'A': CharacterDef(cid='A', name='A', level=10, stats=Stats(str=10, agi=30, con=10, int=10, wis=10, c... flags=set())}, turn_order=['A', 'E'], turn_index=0, tick=0, groups={0: ['A'], 1: ['E']}, ended=False, end_reason=None)
skill = Skill(skill_id='P26_SIMPLE_MAIN', name='simple', actor='A', action_type='MAIN', cooldown_turns=0, steps=[Step(kind='AP...ne, modifier_key=None, modifier_delta=None, modifier_duration=None, hp_delta=-1, require_prev_gte=0)], crit_stat='STR')

    def apply_skill(self, bs: BattleState, skill: Skill, *, reaction_hit_penalty: int = 5) -> EngineOutcome:
        """
        스킬 단위 실행 파이프라인.
        - 슬롯(MAIN/SUB) 소모
        - 쿨다운 확인/등록
        - steps 순차 실행(각 step은 미시 행동)
        """
        events: list[str] = []
    
        # 0) 현재 턴 actor 확인
        actor = bs.current_actor_id()
        if skill.actor != actor:
            raise ValueError("Not your turn (skill.actor != current actor).")
    
        # 1) 슬롯 소모
        if skill.action_type == "MAIN":
            self._use_main(bs, actor)
            events.append(f"SLOT: MAIN used by {actor}")
        else:
            self._use_sub(bs, actor)
            events.append(f"SLOT: SUB used by {actor}")
    
        # 2) 쿨다운 체크
        if skill.cooldown_turns > 0:
            left = bs.combatants[actor].cooldowns.get(skill.skill_id, 0)
            if left > 0:
                raise ValueError(f"Skill on cooldown: {skill.skill_id} (ticks_left={left})")
    
        # 3) 상태이상에 의한 스킬 사용 불가 체크
        ok, reason = self._can_use_skill_due_to_effects(bs, actor, skill)
        if not ok:
            # 기존 Outcome 생성 방식에 맞춰 반환값만 맞춰줘
            events = []
            events.append(f"SKILL_BLOCKED_BY_EFFECT: actor={actor} skill={skill.skill_id} reason={reason}")
>           return EngineOutcome(result=0, events=events)
E           TypeError: EngineOutcome.__init__() got an unexpected keyword argument 'result'

battle_system\engine\engine.py:148: TypeError
============================================================
[FAIL] tests/test_phase26_effects_passive_and_control.py::test_phase26_action_blocking_effects_block_any_skill[Paralysis]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 행동 불가 상태이상은 어떤 스킬이든 사용을 막아야 한다.
    대상:
    - Stun / Paralysis / Frozen

    시나리오:
    - actor(A)에 effect ticks=2 부여
    - '간단 데미지 스킬' 시전 시도
    기대:
    - SKILL_BLOCKED_BY_EFFECT 이벤트가 발생
    - target HP 변화 없음 (실제 step 적용이 일어나지 않음)


[TRACEBACK]
effect_id = 'Paralysis'

    @pytest.mark.parametrize("effect_id", ["Stun", "Paralysis", "Frozen"])
    def test_phase26_action_blocking_effects_block_any_skill(effect_id: str):
        """
        [Phase 26] 행동 불가 상태이상은 어떤 스킬이든 사용을 막아야 한다.
        대상:
        - Stun / Paralysis / Frozen
    
        시나리오:
        - actor(A)에 effect ticks=2 부여
        - '간단 데미지 스킬' 시전 시도
        기대:
        - SKILL_BLOCKED_BY_EFFECT 이벤트가 발생
        - target HP 변화 없음 (실제 step 적용이 일어나지 않음)
        """
        eng, bs, A, E = _mk_engine_1v1()
    
        _set_effect(bs, A, effect_id, ticks=2)
    
        before = bs.combatants[E].hp
        skill = _simple_damage_skill(A, E, action_type="MAIN", crit_stat="STR")
    
>       out = eng.apply_skill(bs, skill)

tests\test_phase26_effects_passive_and_control.py:395: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <battle_system.engine.engine.BattleEngine object at 0x000001DAA25A0A70>
bs = BattleState(defs={'A': CharacterDef(cid='A', name='A', level=10, stats=Stats(str=10, agi=30, con=10, int=10, wis=10, c... flags=set())}, turn_order=['A', 'E'], turn_index=0, tick=0, groups={0: ['A'], 1: ['E']}, ended=False, end_reason=None)
skill = Skill(skill_id='P26_SIMPLE_MAIN', name='simple', actor='A', action_type='MAIN', cooldown_turns=0, steps=[Step(kind='AP...ne, modifier_key=None, modifier_delta=None, modifier_duration=None, hp_delta=-1, require_prev_gte=0)], crit_stat='STR')

    def apply_skill(self, bs: BattleState, skill: Skill, *, reaction_hit_penalty: int = 5) -> EngineOutcome:
        """
        스킬 단위 실행 파이프라인.
        - 슬롯(MAIN/SUB) 소모
        - 쿨다운 확인/등록
        - steps 순차 실행(각 step은 미시 행동)
        """
        events: list[str] = []
    
        # 0) 현재 턴 actor 확인
        actor = bs.current_actor_id()
        if skill.actor != actor:
            raise ValueError("Not your turn (skill.actor != current actor).")
    
        # 1) 슬롯 소모
        if skill.action_type == "MAIN":
            self._use_main(bs, actor)
            events.append(f"SLOT: MAIN used by {actor}")
        else:
            self._use_sub(bs, actor)
            events.append(f"SLOT: SUB used by {actor}")
    
        # 2) 쿨다운 체크
        if skill.cooldown_turns > 0:
            left = bs.combatants[actor].cooldowns.get(skill.skill_id, 0)
            if left > 0:
                raise ValueError(f"Skill on cooldown: {skill.skill_id} (ticks_left={left})")
    
        # 3) 상태이상에 의한 스킬 사용 불가 체크
        ok, reason = self._can_use_skill_due_to_effects(bs, actor, skill)
        if not ok:
            # 기존 Outcome 생성 방식에 맞춰 반환값만 맞춰줘
            events = []
            events.append(f"SKILL_BLOCKED_BY_EFFECT: actor={actor} skill={skill.skill_id} reason={reason}")
>           return EngineOutcome(result=0, events=events)
E           TypeError: EngineOutcome.__init__() got an unexpected keyword argument 'result'

battle_system\engine\engine.py:148: TypeError
============================================================
[FAIL] tests/test_phase26_effects_passive_and_control.py::test_phase26_action_blocking_effects_block_any_skill[Frozen]
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] 행동 불가 상태이상은 어떤 스킬이든 사용을 막아야 한다.
    대상:
    - Stun / Paralysis / Frozen

    시나리오:
    - actor(A)에 effect ticks=2 부여
    - '간단 데미지 스킬' 시전 시도
    기대:
    - SKILL_BLOCKED_BY_EFFECT 이벤트가 발생
    - target HP 변화 없음 (실제 step 적용이 일어나지 않음)


[TRACEBACK]
effect_id = 'Frozen'

    @pytest.mark.parametrize("effect_id", ["Stun", "Paralysis", "Frozen"])
    def test_phase26_action_blocking_effects_block_any_skill(effect_id: str):
        """
        [Phase 26] 행동 불가 상태이상은 어떤 스킬이든 사용을 막아야 한다.
        대상:
        - Stun / Paralysis / Frozen
    
        시나리오:
        - actor(A)에 effect ticks=2 부여
        - '간단 데미지 스킬' 시전 시도
        기대:
        - SKILL_BLOCKED_BY_EFFECT 이벤트가 발생
        - target HP 변화 없음 (실제 step 적용이 일어나지 않음)
        """
        eng, bs, A, E = _mk_engine_1v1()
    
        _set_effect(bs, A, effect_id, ticks=2)
    
        before = bs.combatants[E].hp
        skill = _simple_damage_skill(A, E, action_type="MAIN", crit_stat="STR")
    
>       out = eng.apply_skill(bs, skill)

tests\test_phase26_effects_passive_and_control.py:395: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <battle_system.engine.engine.BattleEngine object at 0x000001DAA257E960>
bs = BattleState(defs={'A': CharacterDef(cid='A', name='A', level=10, stats=Stats(str=10, agi=30, con=10, int=10, wis=10, c... flags=set())}, turn_order=['A', 'E'], turn_index=0, tick=0, groups={0: ['A'], 1: ['E']}, ended=False, end_reason=None)
skill = Skill(skill_id='P26_SIMPLE_MAIN', name='simple', actor='A', action_type='MAIN', cooldown_turns=0, steps=[Step(kind='AP...ne, modifier_key=None, modifier_delta=None, modifier_duration=None, hp_delta=-1, require_prev_gte=0)], crit_stat='STR')

    def apply_skill(self, bs: BattleState, skill: Skill, *, reaction_hit_penalty: int = 5) -> EngineOutcome:
        """
        스킬 단위 실행 파이프라인.
        - 슬롯(MAIN/SUB) 소모
        - 쿨다운 확인/등록
        - steps 순차 실행(각 step은 미시 행동)
        """
        events: list[str] = []
    
        # 0) 현재 턴 actor 확인
        actor = bs.current_actor_id()
        if skill.actor != actor:
            raise ValueError("Not your turn (skill.actor != current actor).")
    
        # 1) 슬롯 소모
        if skill.action_type == "MAIN":
            self._use_main(bs, actor)
            events.append(f"SLOT: MAIN used by {actor}")
        else:
            self._use_sub(bs, actor)
            events.append(f"SLOT: SUB used by {actor}")
    
        # 2) 쿨다운 체크
        if skill.cooldown_turns > 0:
            left = bs.combatants[actor].cooldowns.get(skill.skill_id, 0)
            if left > 0:
                raise ValueError(f"Skill on cooldown: {skill.skill_id} (ticks_left={left})")
    
        # 3) 상태이상에 의한 스킬 사용 불가 체크
        ok, reason = self._can_use_skill_due_to_effects(bs, actor, skill)
        if not ok:
            # 기존 Outcome 생성 방식에 맞춰 반환값만 맞춰줘
            events = []
            events.append(f"SKILL_BLOCKED_BY_EFFECT: actor={actor} skill={skill.skill_id} reason={reason}")
>           return EngineOutcome(result=0, events=events)
E           TypeError: EngineOutcome.__init__() got an unexpected keyword argument 'result'

battle_system\engine\engine.py:148: TypeError
============================================================
[FAIL] tests/test_phase26_effects_passive_and_control.py::test_phase26_bind_blocks_move_skills_but_allows_non_move
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] Bind:
      - 이동이 포함된 스킬 사용 불가
      - 그리고 EVADE -50(지수 보정형)은 별도 테스트에서 검증됨

    시나리오:
    - actor(A)에 Bind ticks=2 부여
    - non-move 스킬은 막히지 않아야 함
    - move 스킬(MOVE step 포함)은 막혀야 함


[TRACEBACK]
def test_phase26_bind_blocks_move_skills_but_allows_non_move():
        """
        [Phase 26] Bind:
          - 이동이 포함된 스킬 사용 불가
          - 그리고 EVADE -50(지수 보정형)은 별도 테스트에서 검증됨
    
        시나리오:
        - actor(A)에 Bind ticks=2 부여
        - non-move 스킬은 막히지 않아야 함
        - move 스킬(MOVE step 포함)은 막혀야 함
        """
        # non-move allowed
        eng1, bs1, A1, E1 = _mk_engine_1v1()
        _set_effect(bs1, A1, "Bind", ticks=2)
        out1 = eng1.apply_skill(bs1, _simple_damage_skill(A1, E1, action_type="MAIN"))
        assert not _is_blocked(out1)
    
        # move blocked
        eng2, bs2, A2, _ = _mk_engine_1v1()
        _set_effect(bs2, A2, "Bind", ticks=2)
>       out2 = eng2.apply_skill(bs2, _move_skill(A2, action_type="MAIN"))

tests\test_phase26_effects_passive_and_control.py:420: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <battle_system.engine.engine.BattleEngine object at 0x000001DAA257F9B0>
bs = BattleState(defs={'A': CharacterDef(cid='A', name='A', level=10, stats=Stats(str=10, agi=30, con=10, int=10, wis=10, c... flags=set())}, turn_order=['A', 'E'], turn_index=0, tick=0, groups={0: ['A'], 1: ['E']}, ended=False, end_reason=None)
skill = Skill(skill_id='P26_MOVE_MAIN', name='move', actor='A', action_type='MAIN', cooldown_turns=0, steps=[Step(kind='MOVE',..., modifier_key=None, modifier_delta=None, modifier_duration=None, hp_delta=None, require_prev_gte=0)], crit_stat='STR')

    def apply_skill(self, bs: BattleState, skill: Skill, *, reaction_hit_penalty: int = 5) -> EngineOutcome:
        """
        스킬 단위 실행 파이프라인.
        - 슬롯(MAIN/SUB) 소모
        - 쿨다운 확인/등록
        - steps 순차 실행(각 step은 미시 행동)
        """
        events: list[str] = []
    
        # 0) 현재 턴 actor 확인
        actor = bs.current_actor_id()
        if skill.actor != actor:
            raise ValueError("Not your turn (skill.actor != current actor).")
    
        # 1) 슬롯 소모
        if skill.action_type == "MAIN":
            self._use_main(bs, actor)
            events.append(f"SLOT: MAIN used by {actor}")
        else:
            self._use_sub(bs, actor)
            events.append(f"SLOT: SUB used by {actor}")
    
        # 2) 쿨다운 체크
        if skill.cooldown_turns > 0:
            left = bs.combatants[actor].cooldowns.get(skill.skill_id, 0)
            if left > 0:
                raise ValueError(f"Skill on cooldown: {skill.skill_id} (ticks_left={left})")
    
        # 3) 상태이상에 의한 스킬 사용 불가 체크
        ok, reason = self._can_use_skill_due_to_effects(bs, actor, skill)
        if not ok:
            # 기존 Outcome 생성 방식에 맞춰 반환값만 맞춰줘
            events = []
            events.append(f"SKILL_BLOCKED_BY_EFFECT: actor={actor} skill={skill.skill_id} reason={reason}")
>           return EngineOutcome(result=0, events=events)
E           TypeError: EngineOutcome.__init__() got an unexpected keyword argument 'result'

battle_system\engine\engine.py:148: TypeError
============================================================
[FAIL] tests/test_phase26_effects_passive_and_control.py::test_phase26_oblivion_blocks_multi_step_only
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] Oblivion:
      - 2스텝 이상 스킬 사용 불가

    시나리오:
    - actor(A)에 Oblivion ticks=2
    - 1-step 스킬은 허용
    - 2-step 스킬은 차단


[TRACEBACK]
def test_phase26_oblivion_blocks_multi_step_only():
        """
        [Phase 26] Oblivion:
          - 2스텝 이상 스킬 사용 불가
    
        시나리오:
        - actor(A)에 Oblivion ticks=2
        - 1-step 스킬은 허용
        - 2-step 스킬은 차단
        """
        # 1-step allowed
        eng1, bs1, A1, E1 = _mk_engine_1v1()
        _set_effect(bs1, A1, "Oblivion", ticks=2)
        out1 = eng1.apply_skill(bs1, _simple_damage_skill(A1, E1, action_type="MAIN"))
        assert not _is_blocked(out1)
    
        # 2-step blocked
        eng2, bs2, A2, E2 = _mk_engine_1v1()
        _set_effect(bs2, A2, "Oblivion", ticks=2)
>       out2 = eng2.apply_skill(bs2, _two_step_skill(A2, E2, action_type="MAIN"))

tests\test_phase26_effects_passive_and_control.py:443: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <battle_system.engine.engine.BattleEngine object at 0x000001DAA25A12B0>
bs = BattleState(defs={'A': CharacterDef(cid='A', name='A', level=10, stats=Stats(str=10, agi=30, con=10, int=10, wis=10, c... flags=set())}, turn_order=['A', 'E'], turn_index=0, tick=0, groups={0: ['A'], 1: ['E']}, ended=False, end_reason=None)
skill = Skill(skill_id='P26_2STEP_MAIN', name='two-step', actor='A', action_type='MAIN', cooldown_turns=0, steps=[Step(kind='A...ne, modifier_key=None, modifier_delta=None, modifier_duration=None, hp_delta=-1, require_prev_gte=0)], crit_stat='STR')

    def apply_skill(self, bs: BattleState, skill: Skill, *, reaction_hit_penalty: int = 5) -> EngineOutcome:
        """
        스킬 단위 실행 파이프라인.
        - 슬롯(MAIN/SUB) 소모
        - 쿨다운 확인/등록
        - steps 순차 실행(각 step은 미시 행동)
        """
        events: list[str] = []
    
        # 0) 현재 턴 actor 확인
        actor = bs.current_actor_id()
        if skill.actor != actor:
            raise ValueError("Not your turn (skill.actor != current actor).")
    
        # 1) 슬롯 소모
        if skill.action_type == "MAIN":
            self._use_main(bs, actor)
            events.append(f"SLOT: MAIN used by {actor}")
        else:
            self._use_sub(bs, actor)
            events.append(f"SLOT: SUB used by {actor}")
    
        # 2) 쿨다운 체크
        if skill.cooldown_turns > 0:
            left = bs.combatants[actor].cooldowns.get(skill.skill_id, 0)
            if left > 0:
                raise ValueError(f"Skill on cooldown: {skill.skill_id} (ticks_left={left})")
    
        # 3) 상태이상에 의한 스킬 사용 불가 체크
        ok, reason = self._can_use_skill_due_to_effects(bs, actor, skill)
        if not ok:
            # 기존 Outcome 생성 방식에 맞춰 반환값만 맞춰줘
            events = []
            events.append(f"SKILL_BLOCKED_BY_EFFECT: actor={actor} skill={skill.skill_id} reason={reason}")
>           return EngineOutcome(result=0, events=events)
E           TypeError: EngineOutcome.__init__() got an unexpected keyword argument 'result'

battle_system\engine\engine.py:148: TypeError
============================================================
[FAIL] tests/test_phase26_effects_passive_and_control.py::test_phase26_curse_blocks_magic_attack_with_apply_effect_but_not_others
------------------------------------------------------------
[EXPERIMENT]
[Phase 26] Curse:
      - "마법 공격" + "상태이상 부여 포함" 스킬 사용 불가
      - 아래는 막히면 안 됨:
        (1) 마법 공격이지만 APPLY_EFFECT 없는 스킬
        (2) APPLY_EFFECT는 있지만 물리 공격(crit_stat=STR/AGI) 스킬

    시나리오:
    - actor(A)에 Curse ticks=2
    - (막힘) magic + apply_effect
    - (허용) magic + no apply_effect
    - (허용) physical + apply_effect


[TRACEBACK]
def test_phase26_curse_blocks_magic_attack_with_apply_effect_but_not_others():
        """
        [Phase 26] Curse:
          - "마법 공격" + "상태이상 부여 포함" 스킬 사용 불가
          - 아래는 막히면 안 됨:
            (1) 마법 공격이지만 APPLY_EFFECT 없는 스킬
            (2) APPLY_EFFECT는 있지만 물리 공격(crit_stat=STR/AGI) 스킬
    
        시나리오:
        - actor(A)에 Curse ticks=2
        - (막힘) magic + apply_effect
        - (허용) magic + no apply_effect
        - (허용) physical + apply_effect
        """
        # (blocked) magic + apply_effect
        eng1, bs1, A1, E1 = _mk_engine_1v1()
        _set_effect(bs1, A1, "Curse", ticks=2)
>       out1 = eng1.apply_skill(bs1, _magic_apply_effect_skill(A1, E1, action_type="MAIN"))

tests\test_phase26_effects_passive_and_control.py:464: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <battle_system.engine.engine.BattleEngine object at 0x000001DAA25A3920>
bs = BattleState(defs={'A': CharacterDef(cid='A', name='A', level=10, stats=Stats(str=10, agi=30, con=10, int=10, wis=10, c... flags=set())}, turn_order=['A', 'E'], turn_index=0, tick=0, groups={0: ['A'], 1: ['E']}, ended=False, end_reason=None)
skill = Skill(skill_id='P26_MAGIC_APPLY_MAIN', name='magic+apply', actor='A', action_type='MAIN', cooldown_turns=0, steps=[Ste..., modifier_key=None, modifier_delta=None, modifier_duration=None, hp_delta=None, require_prev_gte=0)], crit_stat='INT')

    def apply_skill(self, bs: BattleState, skill: Skill, *, reaction_hit_penalty: int = 5) -> EngineOutcome:
        """
        스킬 단위 실행 파이프라인.
        - 슬롯(MAIN/SUB) 소모
        - 쿨다운 확인/등록
        - steps 순차 실행(각 step은 미시 행동)
        """
        events: list[str] = []
    
        # 0) 현재 턴 actor 확인
        actor = bs.current_actor_id()
        if skill.actor != actor:
            raise ValueError("Not your turn (skill.actor != current actor).")
    
        # 1) 슬롯 소모
        if skill.action_type == "MAIN":
            self._use_main(bs, actor)
            events.append(f"SLOT: MAIN used by {actor}")
        else:
            self._use_sub(bs, actor)
            events.append(f"SLOT: SUB used by {actor}")
    
        # 2) 쿨다운 체크
        if skill.cooldown_turns > 0:
            left = bs.combatants[actor].cooldowns.get(skill.skill_id, 0)
            if left > 0:
                raise ValueError(f"Skill on cooldown: {skill.skill_id} (ticks_left={left})")
    
        # 3) 상태이상에 의한 스킬 사용 불가 체크
        ok, reason = self._can_use_skill_due_to_effects(bs, actor, skill)
        if not ok:
            # 기존 Outcome 생성 방식에 맞춰 반환값만 맞춰줘
            events = []
            events.append(f"SKILL_BLOCKED_BY_EFFECT: actor={actor} skill={skill.skill_id} reason={reason}")
>           return EngineOutcome(result=0, events=events)
E           TypeError: EngineOutcome.__init__() got an unexpected keyword argument 'result'

battle_system\engine\engine.py:148: TypeError
